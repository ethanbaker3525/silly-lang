add type checking before compilation/ interpreting
add match
add type definitions
add type anotations
add compilation for the ops added to interp
add compilation for vars and let vars
add compilation for funcs and let funcs
add side effecty ops like input and print (reading/ writing from/ to files)
    - THINK ABOUT HOW YOU WANT TO DO IMPORTING/ ENCAPSULATION
add helpful error messages and line backtracking (can be done with ast and lark)
add qol features to shell 
    - special parser that doesnt err when there is no final expr
    - refactor shell so its not such a pain, todos are in .py file
add robust testing